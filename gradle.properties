# Done to increase the memory available to gradle.
# Optimized for Java 21 with G1GC and improved garbage collection
org.gradle.jvmargs=-Xmx4G -Xms2G -XX:+UseG1GC -XX:MaxGCPauseMillis=200 -XX:+UseStringDeduplication
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.vfs.watch=true
org.gradle.daemon=true
org.gradle.configureondemand=true
# Worker threads for parallel execution (optimized for available cores)
org.gradle.workers.max=6

# Fabric Properties
# check these on https://fabricmc.net/develop
minecraft_version=1.21.10
yarn_mappings=1.21.10+build.1
loader_version=0.17.2
loom_version=1.11-SNAPSHOT
fabric_version=0.134.1+1.21.10

# Mod Properties
mod_version=0.96-dev-Pet-this-Pet-that
maven_group=woflo.petsplus
archives_base_name=petsplus
